# 🚀 Быстрый старт

## Установка зависимостей

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости с правильными версиями
pip install -r requirements.txt
```

Если возникают ошибки с `redis`:
```bash
pip install 'redis[hiredis]'
```

Если возникают ошибки с `g4f`:
```bash
pip install --upgrade g4f
```

## Настройка .env

```bash
# Скопируйте пример
cp env.example .env

# Добавьте токен бота
# Отредактируйте .env и добавьте:
TOKEN=your_bot_token_here
```

## Инициализация БД

```bash
python init_db.py
```

## Запуск

```bash
python main.py
```

## Проверка работы

1. Найдите бота в Telegram
2. Отправьте `/start`
3. Задайте вопрос: "Объясни мне дроби"

---

## Решение проблем

### Ошибка: "AsyncResult" не является awaitable

Эта ошибка была исправлена в `fetch/g4f.py`. Теперь используется правильный асинхронный вызов через `loop.run_in_executor()`.

### Ошибка: redis.asyncio не найден

Установите правильную версию:
```bash
pip install 'redis[hiredis]'
```

### Ошибка: БД не создана

Запустите:
```bash
python init_db.py
```

### Бот не отвечает

1. Проверьте что токен в `.env` правильный
2. Проверьте что Redis запущен (опционально):
```bash
redis-cli ping
# Должен вернуть: PONG
```

Если Redis не установлен - бот будет работать без контекста сообщений.

### AI не отвечает

Если g4f не работает, это может быть временная проблема с провайдерами. В этом случае бот отправит сообщение об ошибке пользователю.

---

**Бот готов к использованию!** 🎉

