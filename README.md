# 🤖 Ayana - Образовательный Telegram Бот

Ayana — это образовательный бот, созданный для помощи детям получать дополнительные знания и практику с интересом.

## 📋 Описание

Бот Ayana помогает компенсировать недостаток индивидуального внимания в переполненных классах, предоставляя детям:
- Дополнительную практику по различным предметам
- Объяснения сложных тем в доступной форме
- Интерактивное обучение через диалог
- Адаптацию сложности под уровень ребенка

## 🚀 Возможности

- 💬 Интерактивный чат с AI для получения ответов на вопросы
- 📚 Адаптивное обучение под уровень ребенка
- 💾 Сохранение истории диалогов
- 🔄 Контекстная память беседы
- 🎯 Извлечение информации о ребенке для персонализации

## 🛠 Установка и Настройка

### Быстрый старт с Docker (Рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd ayana-tel

# 2. Создайте .env файл
cp env.example .env

# 3. Добавьте токен бота
nano .env  # Добавьте: TOKEN=your_bot_token_here

# 4. Запустите бота
./start.sh

# Или вручную:
docker-compose up -d
```

Подробная инструкция: [START.md](START.md)

### Требования (для установки без Docker)

- Python 3.11+
- PostgreSQL (опционально, по умолчанию используется SQLite)
- Redis (опционально, для кэширования контекста)

### Шаги установки (Без Docker)

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ayana-tel
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # На Linux/Mac
# или
venv\Scripts\activate  # На Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**
```bash
cp .env.example .env
```

5. **Настройте переменные окружения в `.env`:**
```bash
TOKEN=your_telegram_bot_token_here
```

Получить токен можно у [@BotFather](https://t.me/BotFather) в Telegram.

6. **Инициализируйте базу данных:**
```bash
python -c "from models.base import engine, Base; import asyncio; asyncio.run(Base.metadata.create_all(engine))"
```

7. **Запустите бота:**
```bash
python main.py
```

## 📁 Структура проекта

```
ayana-tel/
├── config/          # Конфигурация приложения
├── fetch/           # Интеграция с AI (g4f)
├── models/          # Модели базы данных (SQLAlchemy)
├── routers/         # API роутеры (FastAPI)
├── service/         # Бизнес-логика
├── ui/              # UI компоненты (кнопки, окна)
├── utils/           # Утилиты (Redis, извлечение данных)
├── txt/             # Текстовые файлы (help, about, guide)
└── main.py          # Точка входа приложения
```

## 🔧 Настройка базы данных

### SQLite (по умолчанию)

Просто запустите бота - база данных создастся автоматически в `local.db`.

### PostgreSQL

1. Установите PostgreSQL
2. Создайте базу данных:
```bash
createdb ayana_db
```

3. Обновите `.env`:
```env
USE_POSTGRES=true
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ayana_db
```

## 🔧 Настройка Redis

Redis используется для хранения контекста диалогов. Если Redis не доступен, бот будет работать, но контекст не будет сохраняться.

```bash
# Установка Redis (Ubuntu/Debian)
sudo apt install redis-server
sudo systemctl start redis

# Или через Docker
docker run -d -p 6379:6379 redis
```

## 📝 Команды бота

- `/start` - Начать работу с ботом
- `/menu` - Открыть главное меню
- `/help` - Получить помощь
- `/about` - Информация о боте
- `/gyde` - Руководство по использованию
- `/set_name Имя` — сохранить имя ребенка (например: `/set_name Алиса`)
- `/set_age Число` — сохранить возраст (например: `/set_age 8`)

## 🧪 Тестирование

Для проверки работоспособности запустите бота и отправьте ему сообщение в Telegram:

```
/start
```

Затем попробуйте задать вопрос, например:
```
Объясни мне дроби
```

Профиль ребенка повышает качество ответов:
```
/set_name Алиса
/set_age 8
```

## 🐳 Docker (опционально)

Для запуска в Docker:

```bash
docker build -t ayana-bot .
docker run -d --env-file .env ayana-bot
```

## 📚 API Endpoints (FastAPI)

Если нужен REST API:

```python
# Запуск FastAPI сервера
uvicorn main:app --host 0.0.0.0 --port 8000
```

Доступные эндпоинты:
- `POST /api/v1/chat/send` - Отправить сообщение
- `GET /api/v1/chat/history` - Получить историю диалога

## 🤝 Вклад в проект

Мы приветствуем любые улучшения! Для этого:

1. Сделайте Fork проекта
2. Создайте ветку для новой функции
3. Внесите изменения и создайте Pull Request

## 📄 Лицензия

Этот проект создан школьниками для школьников.

## 👥 Разработчики

- Команда разработчиков Ayana
- Контакт: [@cheloberockk](https://t.me/cheloberockk)

---

**Сделано с ❤️ школьниками, для школьников**

